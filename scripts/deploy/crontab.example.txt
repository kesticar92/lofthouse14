# Ejemplo: crontab en el Droplet (crontab -e)
# Reemplaza DOMINIO y CRON_SECRET.
#
# */5 * * * * curl -fsS -H "Authorization: Bearer CRON_SECRET" https://DOMINIO/api/cron/sync-ical >/dev/null 2>&1
# 0 11 * * * curl -fsS -H "Authorization: Bearer CRON_SECRET" https://DOMINIO/api/cron/cleaning-sync >/dev/null 2>&1
# 15 */6 * * * curl -fsS -H "Authorization: Bearer CRON_SECRET" https://DOMINIO/api/cron/expense-drive-retry >/dev/null 2>&1
